{
  "mcpServers": {
    "filesystem-with-morph": {
      "env": {
        "MORPH_API_KEY": "${MORPH_API_KEY}",
        "ENABLED_TOOLS": "all"
      },
      "command": "npx",
      "args": ["-y", "@morphllm/morphmcp"]
    }
  }
}
